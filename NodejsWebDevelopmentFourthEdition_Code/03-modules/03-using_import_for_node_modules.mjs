/*
 * Node.js modules can be used from ES2015 .mjs code
 *
 * You cannot, however, require an ES2015 module into regular Node.js code.
 *
 */
import fs   from 'fs';
import util from 'util';
// const fs   = require('fs');
// const util = require('util');


const fs_readdir = util.promisify( fs.readdir );

(async () =>
{
  const files = await fs_readdir('.');
  for (let fn of files) {
    console.log(fn);
  }

})().catch(err => {
  console.error(err);
});
